  <div class="container px-5 px-sm-0">

		<div class="row">
			<div class="col-md-3">
				<%= render 'users/user_show', user: @user %>
				<%= render 'books/book_new', book: @book %>
			</div>
		
		
		
		
		
		<!--<% @users.each do |user| %>-->
		<!--	<div>-->
			  
		<!--	  <p>image：<%= image_tag user.get_profile_image(100,100) %></p>-->
		<!--	  <p>name：<%= user.name %></p>-->
			  
		<!--	  <%= link_to "Show", user_path(user.id) %>-->
		<!--	</div>-->
		<!--<% end %>-->
		
		<div class="col-md-8 offset-md-1">
		
			<h1>Users</h1>
			
			<table class="table">
				<thead>
					<tr>
						<th>image</th>
						<th>name</th>
						<th colspan="3"></th>
					</tr>
				</thead>
				<tbody>
					<% @users.each do |user| %>
					<tr>
						<td><p><%= image_tag user.get_profile_image(40,40) %></p></td>
						<td><p><%= user.name %></p></td>
						<td><%= link_to "Show", user_path(user.id) %></td>
					</tr>
					<% end %>
				</tbody>
			</table>
			
		</div>
		</div>
	</div>